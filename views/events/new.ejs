<!DOCTYPE html>
<html>
	<head>
		<% include ../partials/head.ejs %>
    <% include ../partials/nav.ejs %>
		<title>New Event</title>
	</head>
	<body>
		<header>
			<h2>Create an Event</h2>
			<nav>
				<ul>
					<li>
						<a href="/events">Events Index</a>
					</li>
				</ul>
			</nav>
		</header>
		<main>
			<form action="/events" method="post">
				<label>Title:</label><input type="text" name="title"/><br/>
				<label>Date:</label><input type="date" name="date"><br/>
				<label>Time:</label><input type="time" name="time"><br/>
				<!-- todo: only it this is the Admin -->
					<% if (volunteers.length > 0) { %>
						<br/><br/><br/><label>Volunteers:</label><select name="volunteerId">
	                    <% for(let i = 0; i < volunteers.length; i++) { %>
	                        <option value="<%=volunteers[i]._id%>"><%=volunteers[i].name%></option>
	                    <% } %>
	                </select><br/><br/>
					<% } %>
				<input class="button-primary submit-button" type="submit" value="Publish Event"/>
			</form>
		</main>
		<% include ../partials/footer.ejs %>
	</body>
</html>
